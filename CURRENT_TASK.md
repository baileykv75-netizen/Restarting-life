# 当前任务：UX-02B 青霞地界地图美术化与地点视觉重构

## 背景

UX-02A 已经把成年自由行动从三栏 Web Dashboard 改成固定游戏壳层，并通过真实 Pages artifact 试玩：桌面与移动端主页面不再整页滚动，人物 / 背包 / 修炼 / 此世记改为 drawer / bottom sheet，普通旅行抵达后也不再出现空结果页。

现在剩下的核心问题集中到地图本身：现有地图虽然可点击，但仍然像“节点关系图”，缺乏真实地理、区域差异、道路与未知感。

因此继续暂停 R27，先把地图本身做成游戏主舞台。

**本轮不改变 R22～R26 authoritative state、旅行规则、地点知识规则、数值或存档结构。**

---

## 本轮目标

把青霞地界从：

```text
空背景 + 直线 + 长方形文字节点
```

改成：

```text
地形层
+ 山脉 / 河流 / 林谷 / 田地
+ 按玩家知识显示的道路
+ 未知区域雾层
+ 地点标记
+ 点击后浮层查看地点 / 路程 / 确认前往
```

地图本身必须看起来像一个世界，而不是系统拓扑图。

---

## 地理结构

沿用现有 authoritative mapPosition，不重写地点逻辑：

- 北部：万兽岭，高山与妖兽活动区域；
- 中上：青云宗所在灵山；
- 西部：黑风山与山脚，矿山、旧矿道、深色山地；
- 东部：灵溪谷与陆家庄，溪流、林谷、药田；
- 中部：青霞坊市；
- 南部：青石镇、白石村、临河县；
- 青云宗家属区作为宗门外围子地点。

---

## 信息规则

- 已发现地点：显示真实地点标记与名称；
- 传闻地点：显示弱化标记与“传闻”状态；
- 未知地点：不显示名称、图标和路线；
- 未知方向只允许用云雾表现，不泄露具体地点；
- 道路只使用 getVisibleWorldConnections()；
- travel / fast travel 仍走原 engine；
- 地图 UI 不保存第二套 authoritative 地点状态。

---

## 交互

默认进入地图时不常驻地点说明卡。

```text
点击地点
→ 地点标记高亮
→ 地图内浮出地点卡
→ 显示地点性质 / 危险 / 简述 / 路程
→ 前往此地
→ 正常抵达后回地图
```

当前位置的行为继续放在地图下方的情境行动栏。

当前宗门事务移动到地图内紧凑追踪器，不再额外把地图向下撑长。

---

## 美术资产策略

已经尝试让 GPT 生图直接生成整张无文字底图，但模型连续把地图补成带 HUD / 汉字 / 按钮的完整 UI 截图，不适合作为生产底图。

因此第一版采用：

- 可控 SVG 地形层；
- React 真实地点标记；
- CSS 统一水墨 / 地志风格；
- 后续只针对独立小资产生图，例如宗门建筑、坊市、山峰、树林纹理；
- 生成资产不得包含伪 UI、伪文字或写死的状态信息。

---

## 验收标准

- Typecheck 通过；
- Vitest 全量通过；
- Production build 通过；
- Pages 部署成功；
- 桌面与 390px 移动端实际试玩；
- 地图仍使用原 mapPosition / knowledge / travel state；
- 未知地点不被 UI 泄露；
- 可见道路与已有 knowledge 一致；
- 地图节点可点击；
- 地点浮层可关闭；
- direct / fast travel 路程正确；
- 正常旅行继续不出现空结果页；
- 地图默认视图不再像空白节点关系图；
- 地点详情与当前事务不会把地图主舞台长期向下顶开。

**UX-02B 实际试玩通过前仍不开始 R27。**
