# 当前任务：UX-01 主界面信息层级与交互地图重构

## 为什么插入这一轮

R24～R26 完整人生试玩暴露出明显的 UI 问题：功能已经能跑，但世界页把人物、地图、地点说明、宗门、事务、师承、门规、探索、修炼、背包和《此世记》同时摊开，页面越来越长，也越来越像系统后台。

因此在 R27 炼丹之前先完成一次纯交互层重构。

**本轮不新增玩法系统，不改 R22～R26 的底层规则、数值、存档结构和 authoritative state。**

---

# 一、本轮目标

把成年世界界面改成：

```text
地图负责“去哪”
当前位置负责“在这里做什么”
人物 / 背包 / 修炼 / 此世记按需展开
宗门内部功能按地点入口展开
```

默认页面只呈现当前决策需要的信息。

---

# 二、交互地图

地图节点必须真正可点击。

玩家流程：

```text
点击地点
→ 地点高亮
→ 出现地点卡
→ 显示地点性质 / 危险 / 路程耗时
→ 玩家确认前往
→ 复用现有 travelEngine / fast travel
```

规则：

- 当前地点点击后显示当前位置提示，不产生旅行；
- 已发现地点若有直接路线，显示真实 direct travel 天数；
- 已发现地点若只能沿走熟路线到达，显示 fast travel 天数；
- 传闻地点可以查看传闻，但不能直接导航；
- 未知地点继续不显示；
- 删除地图下方重复的“前往某地”长按钮列表。

---

# 三、当前位置行动

世界页不再默认同时展开全部系统。

根据当前位置只显示少量入口：

- 地点详情；
- 野外：探索此地、已知地点；
- 青云宗：宗门、事务、师承与门规；
- 有 active 宗门事务时：显示紧凑任务追踪器。

同一时间只展开一个当前位置模块。

---

# 四、常驻信息压缩

## 人物

默认只保留：

- 姓名；
- 境界；
- 年龄；
- 灵石；
- 修为；
- 伤势 / 中毒提示；
- 当前或旧宗门身份摘要。

灵根、寿元、装备、出身、体质、天赋、详细属性、师承记录等点击“查看人物详情”后再展开。

## 背包

默认只显示：

- 已用 / 总槽位；
- 物品种类数；
- 是否存在伤势 / 中毒；
- “打开背包”按钮。

物品说明、装备、治疗、丢弃等只有打开后才显示。

## 修炼

默认只显示：

- 当前境界与进度；
- 当前地点修炼环境；
- 当前主修功法；
- “展开修炼”按钮。

功法列表、改修代价、效率来源、修炼时长、辅修与术法只有展开后显示。

## 此世记

默认只显示最近 3 段经历；完整时间线点击后查看。

---

# 五、宗门界面收敛

青云宗不再默认显示 8 行权限矩阵。

改成：

- 当前身份；
- 现在能去的宗门地点；
- 下一层被拦住的地方；
- 实际需要操作时再进入传功、事务等模块。

“师承与门规”内部再拆成：

```text
师承 / 门规 / 离宗
```

避免三块内容同时展开。

玩家文案避免：

- “系统不会……”；
- “本版本……”；
- “不会出现第五阶段……”；
- “不另造某某点数……”；
- 其他开发说明式表述。

---

# 六、验收

本轮至少满足：

- TypeScript typecheck 通过；
- Vitest 全量通过；
- Vite build 通过；
- 原存档可继续加载；
- travel / fast travel 仍复用原逻辑；
- 地图节点点击后能正确显示目的地与耗时；
- 地图下方不再重复铺旅行列表；
- 默认世界页明显变短；
- 人物、背包、修炼、此世记默认均为摘要态；
- 青云宗默认不再是一整张权限表；
- 宗门任务、拜师、违规、叛宗底层结果不变。

UX-01 合并并部署后，继续执行 R24～R26 完整人生试玩验收。

**试玩通过前仍不开始 R27。**
