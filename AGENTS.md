# AGENTS.md

本文件是《此世问长生》V2.0 的长期开发纪律。除非用户明确要求修改本文件，否则所有后续 Codex / Agent 开发都必须遵守。

## 1. 文档优先级

发生冲突时按以下顺序执行：

1. `CURRENT_TASK.md`：当前轮唯一施工范围；
2. `V2_GAME_DESIGN.md`：V2.0 玩法唯一真源；
3. `V2_CONTENT_BIBLE.md`：V2.0 首个完整可玩版本的内容真源；
4. `V2_MIGRATION_AUDIT.md`：V1.2 → V2.0 迁移边界；
5. `V2_GITHUB_ROADMAP.md`：总开发顺序；
6. `HANDOFF.md`：当前进度、已知问题和上一轮交接；
7. `README.md`：项目入口说明；
8. V1 / V1.1 / V1.2 文档：只作为历史参考，不得覆盖 V2 决策。

如果 `CURRENT_TASK.md` 与 `V2_GAME_DESIGN.md` 明显矛盾，不得自行猜测扩大任务；应采用最小兼容实现，并在交付说明中指出冲突。

如果当前轮涉及具体出身、地点、人物、功法、物品、妖兽、事件、资源或世界设定，必须先查 `V2_CONTENT_BIBLE.md`。不得用路线文档中的旧示例名、临时占位内容或 Codex 自行生成内容覆盖已冻结设定。

## 2. 一轮只做一件事

- 每轮只实现 `CURRENT_TASK.md` 中明确列出的目标。
- 未被 `CURRENT_TASK.md` 点名的系统默认不得修改。
- 禁止“顺手优化”“顺便重构”“顺便补下一阶段”。
- 如果完成当前目标必须修改额外文件，只允许做最小必要修改。
- 任何大重构必须单独成为一个明确轮次，不能藏在普通功能轮中。

## 3. V2 迁移原则

- 不重开新仓库。
- 不创建长期并行的 `src/v2/`、`GameStateV2` 第二套玩法体系。
- 现有 `GameState`、Session / Command、Seeded RNG、存档校验、Chronicle、测试体系优先保留并逐步扩展。
- 新系统先上线并验证，旧 V1.2 主循环再退役。
- 旧 `ActionPanel`、`actionEngine` 的 explore/livelihood 抽事件职责、全局 `FORMAL_EVENTS` 主循环在迁移完成前可继续兼容，但不得继续扩张为 V2 正式玩法。
- 不强行把正在进行的 V1.2 人生补字段迁移成 V2 世界。

## 4. 状态纪律

- 所有角色与世界核心状态必须有唯一权威来源：`GameState` / `PersistentGame`。
- 页面组件不得长期维护与 `GameState` 冲突的第二套核心状态。
- UI 不得绕过规则引擎直接修改核心状态。
- 时间统一使用 `worldDay`；不得新增另一套权威月份 / 年份计时。
- 静态定义与运行态分离：地点、物品、功法、事件模板等静态定义不整份写入存档。
- 世界真相与角色认知分离；隐藏状态不得为了方便 UI 被直接暴露。

## 5. 内容与代码分离

- 规则代码放在 core / engine 层；内容定义放在 data 层。
- 具体世界内容优先来源于 `V2_CONTENT_BIBLE.md`；未冻结内容不得由 Codex 自行批量扩写并反向固化为正式设定。
- 不允许为了单个事件不断加入特殊 hardcode。
- 优先建立可复用 condition / effect / resolver。
- 新内容应优先改变选择、世界入口、资源、地点、关系或规则，而不是只加属性数值。

## 6. 测试与交付

每轮完成前必须：

1. 运行 `npm run typecheck`；
2. 运行 `npm test`；
3. 运行 `npm run build`；
4. 确认首页可打开；
5. 确认本轮核心流程可实际完成；
6. 确认刷新后关键状态不丢失；
7. 确认无明显 console error；
8. 确认上一轮已验收功能未退化；
9. 更新 `HANDOFF.md`。

构建或测试失败时不得把该轮标记为完成。

## 7. 修复轮

出现以下任一问题时，应停止新增功能并进入修复轮：

- 白屏；
- build / typecheck / test 失败；
- 存档损坏；
- 核心状态不同步；
- 时间错误；
- 资源复制或丢失；
- 刷新后人生错乱；
- 新系统绕开统一状态；
- 上一轮功能退化；
- 试玩证明当前实现方向错误。

修复轮只修问题，不新增功能。

## 8. 禁止假完成

- 不允许以 TODO、占位按钮、未接入假数据冒充当前任务完成。
- 不允许复制第二套逻辑来绕过现有架构问题。
- 不允许为追求事件数量牺牲一世完整闭环。
- 不允许实时 LLM 生成成为首版核心玩法依赖。
- R32 完成前优先保证闭环，不优先扩充高境界、大地图、第二宗门、完整魔宗、动态经济、家庭经营等内容。

## 9. 交付说明格式

每轮结束至少说明：

- 本轮完成了什么；
- 修改了哪些关键文件；
- 运行了哪些测试；
- 是否存在已知问题；
- `HANDOFF.md` 已如何更新；
- 下一轮应从哪里开始。
