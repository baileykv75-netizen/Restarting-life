# HANDOFF.md

# 《此世问长生》V2.0 当前交接

## 当前状态

- 当前开发主线：**R16「基础修炼」已完成，下一轮进入 R17「功法系统」。**
- R00.1～R00.3：迁移、存档 V3 与开发规则完成。
- R01：唯一 `GameState` 完成。
- R02：统一 `GameAction / SessionCommand / reducer / replay` 边界完成。
- R03：V3 单档自动保存 / 恢复完成。
- R04：V2 Game Shell 完成。
- C00：`V2_CONTENT_BIBLE.md` 成为首版具体内容真源。
- R05：出生三选一完成。
- R06：8 出身 × 2 童年关键节点完成。
- R07：成年 / 入道入口完成。
- R08：11 个固定世界节点与当前地点初始化完成。
- R09：地点知识 `Unknown → Rumored → Discovered` 与认知地图完成。
- R10：固定路线、逐节点旅行、旅行时间、已走路线与快速前往完成。
- R11：黑风山 / 灵溪谷 / 万兽岭固定区域探索完成。
- R12：每世有限随机子地点、确定性生成、探索发现与保存 / replay 完成。
- C13：首版第一秘境「沉脉石室」内容冻结完成。
- R13：沉脉石室五节点、一次性资源、不可回头核心、临时生死解析、永久清空完成。
- R14：正式背包、R13 材料接管、容量 / 堆叠 / 丢弃 / 储物袋扩容完成。
- R15：四槽装备、装备 / 卸下、库存引用、丢弃保护、阶 + 品结构完成。
- C16：炼气环境、炼气九层意义、炼气→筑基、筑基→金丹、三条筑基后传承正式冻结。
- R16：正式基础修炼、主修选择、1 / 3 / 10 / 30 日修炼、引气入体、炼气 1～9 层推进完成。
- legacy Action/Event/Result/End 只为旧档、旧测试与迁移兼容保留，不得继续扩张。

---

## 内容真源与仍待后续补齐的缺口

具体出身、地点、人物、功法、物品、妖兽、事件与世界设定以 `V2_CONTENT_BIBLE.md` 为准。

C16 已解决：

- 炼气 → 筑基具体准备、资源、时间、成功修正与失败后果；
- 筑基 → 金丹具体准备、资源、时间、成功修正与失败后果；
- 青云宗正统、散修 / 遗迹、邪道三条筑基至金丹前主修延续。

仍需在真正依赖它们的轮次前补齐，不得为清 TODO 提前硬编：

1. **2～3 个具体延寿物**：在延寿资源第一次进入重大机缘、交易、秘境或寿元决策玩法前冻结；
2. **随机子地点正式内容池 8～12 个**：R12 目前仍只有洞府 / 药谷 / 兽巢 / 遗迹四类运行 archetype；
3. 如首版确实需要，再冻结第 2 个小秘境；
4. **8～12 个重大机缘具体内容**：C30 前补；
5. **30 个普通事件正式正文**：进入普通事件 / 世界事件内容轮前补；
6. **青锋剑、黑铁重剑、赤纹刀、青竹灵弓、黑铁护甲、青狼软甲、护心镜、镇灵玉、流云靴、寻灵盘的具体“阶 + 品”**：在装备第一次真正进入战斗 / 交易 / 制造平衡前统一冻结；
7. **正式伤势 runtime 与修炼的连接**：C16 已规定轻伤 / 重伤 / 经脉伤会影响修炼与突破，但截至 R16 仓库仍没有 authoritative injury runtime。R16 没有用 flags 伪造第二套伤势系统；等正式伤势状态进入唯一 GameState 后，再接入修炼效率与突破判定。

这些缺口不得由实现轮临时编造后反向固化。

---

# 当前关键世界 / 系统事实

## 世界与探索

- 青霞地界固定世界骨架已经存在；玩家知识与世界真相严格分离。
- 黑风山、灵溪谷、万兽岭支持 1 / 3 / 10 日探索。
- R12 每世生成 4～6 个有限随机子地点，未发现前不泄露。
- 首个秘境固定为黑风山「沉脉石室」，已经完成最小闭环。

## 背包与装备

- `GameState.inventory` 与 `GameState.equipment` 都保持 optional，分别由显式 SessionCommand materialize。
- 基础背包 12 槽，小型储物袋有效提升至 24 槽；大型材料真实占多槽。
- 装备只引用 inventory 中真实拥有的物品，不存在第二库存。
- 首版四槽：主武器 / 护甲 / 护身法器 / 辅助法器。
- 十件装备具体品阶仍未冻结，UI 显示“品阶未标定”。

## C16 突破内容

### 炼气 → 筑基

- 新资源：`凝基丹`，二阶下品，约 180～260 下品灵石；高价值但非强制门票。
- 正式突破按 14 日结算。
- 可见修正来自灵根 / 主修契合、功法状态、伤势 / 经脉、静心守一、环境、破障丹、凝基丹、灵石投入、师长指点。
- 允许裸冲；失败分轻 / 重 / 极端，极端可死亡；无额外突破冷却。

### 筑基 → 金丹

- 新资源：`抱元丹`，二阶上品，约 650～900 下品灵石；高价值但不保证成功。
- 正式结丹按 60 日结算。
- 三条筑基后主修延续：`《青元归真经》`、`《归元守一篇》`、`《阴髓录·凝煞篇》`。
- 普通常规金丹不建立雷劫系统。

---

# R16｜基础修炼

## 1. 唯一状态

R16 没有创建 `CultivationStateV2`。继续扩展唯一：

```ts
state.cultivation
```

新增 optional 字段：

```ts
practiceInitialized?: true
knownTechniqueIds?: string[]
mainTechniqueId?: string | null
```

`createInitialGameState()` 不自动补这些字段，因此 R05～R15 旧 schema-3 状态与 replay digest 保持兼容。

`resources.cultivation` 继续是唯一当前小阶段修为进度：

```text
1000 点 = 当前阶段 100.0%
```

## 2. R07 功法入口落地

只有 R07 已经真正获得功法初步传授的 `cultivation_method_access_seed` 才会变成 R16 已知功法：

```text
xiaozhoutian_tuna   → 《小周天吐纳法》
qingyuan_yinqi      → 《青元引气诀》
xie_basic_qi_method → 兼容映射《小周天吐纳法》
lu_basic_qi_method  → 兼容映射《小周天吐纳法》
```

谢家 / 陆家的泛化 seed 没有反向创造新功法名称。

只有 `adult_access_seed` 的宗门招录、坊市接触、散修人脉、黑风山异常线索等，不会自动变成功法。

无灵根人生可以初始化修炼实践状态，但不会出现普通吐纳按钮。

## 3. R16 六门静态主修定义

`src/data/techniques.ts` 当前登记：

1. 《小周天吐纳法》：1.00，通用；
2. 《青元引气诀》：1.08，通用；
3. 《春木养元功》：1.04，木；
4. 《赤阳诀》：1.08，火；
5. 《寒水经》：1.04，水 / 冰；
6. 《厚土养气篇》：1.02，土。

R16 没有新增这些功法的购买 / 掉落 / 家族赠送来源；正常新人生当前主要直接掌握小周天或青元。

## 4. 确定性修炼公式

不使用 RNG：

```text
gain = floor(
  days
  × 4
  × spiritRootMultiplier
  × techniqueEfficiency
  × affinityMultiplier
  × environmentMultiplier
  × traitMultiplier
)
```

灵根倍率直接复用现有 `spiritRoots.ts`，没有第二张资质表。

功法契合：

- 通用功法：1.00；
- 属性契合：1.15；
- 属性不契合：0.85；
- 寒水经把冰灵根也视为契合。

地点 `qiDensity`：

```text
none   0.55
thin   0.70
low    0.80
medium 1.00
high   1.15
```

黑风山 / 万兽岭不会因为危险额外增加修炼收益。

青云宗 static `high` 只有已有正式青云身份事实时才完整使用；普通访客按 medium 的宗门外围环境结算，避免白用核心灵脉。

R16 接入的现有特质只有：

- 静心守一：本次修炼 ≥10 日时 ×1.08；
- 赤阳灵体 + 赤阳诀：×1.10；
- 玄阴灵体 + 寒水经：×1.10。

不相关天赋 / 体质没有硬塞成修炼速度加成。

## 5. 时间、境界与寿终

正式时长：

```text
1 / 3 / 10 / 30 日
```

全部推进唯一 `worldDay`。

凡人有灵根 + 已知主修：累计 1000 点自然完成引气入体，进入炼气一层，溢出修为继续保留。

炼气 1～8 层：每满 1000 点自然进入下一小层。

炼气九层：最多 1000 点，即 100%；之后普通修炼直接拒绝，不自动筑基，不调用 legacy 突破。

闭关期间若寿元耗尽：死亡优先，不结算本次修为、不晋层、不生成成功结果卡。

## 6. Session / save / replay

正式命令：

```text
initialize-cultivation
select-main-technique(techniqueId)
cultivate-days(1 | 3 | 10 | 30)
```

全部进入：

```text
SessionCommand
→ cultivation resolver
→ 唯一 GameState
→ debug log / digest / replay
→ PersistentGame
→ auto-save
```

如果已有结果卡未确认，新的 R16 修炼命令明确拒绝 `RESULT_PENDING`，不会静默吞掉上一条结果。

`saveRepository` 会独立拷贝 `knownTechniqueIds`。

## 7. UI

正式成年世界页增加 `CultivationPanel`：

- 当前境界；
- 当前阶段百分比；
- 已知主修；
- 当前主修；
- 当前地点灵气；
- 可解释效率来源；
- 1 / 3 / 10 / 30 日按钮；
- 预计推进使用 resolver 同一个纯计算函数。

无功法时显示“你还没有一门真正可以开始吐纳的主修功法”。

无灵根时说明普通吐纳无法引气，不出现假按钮。

炼气九层 100% 时只提示需要准备筑基，没有突破按钮。

普通修炼不逐次写 Chronicle；只有首次引气入体留下 1 条 notable 记录。

## 8. 重要 bootstrap 时序保护

R06 结束时角色已经是 `adult`，但此时 R07 成年方向还未必选择。

如果 R16 在这个时点自动初始化，会把“尚未选成年方向”误写成“本世没有功法”，后续即使选择家传 / 青元路线也无法重新解析。

因此 App 当前明确要求：

```text
adultEntry.resolved === true
```

之后才自动执行 `initialize-cultivation`。

这个保护不得在 R17 重构时删除。

## 9. Legacy 兼容

旧 `performBasicCultivation()`、legacy ActionPanel 的修炼行为保留原语义，只为 `legacy-adult` / 旧测试兼容。

R16 没有把新功法 / 地点修炼公式反向塞进旧四按钮主循环。

## 10. 测试与 CI

专项测试覆盖：

- 旧状态 optional 兼容；
- 四种 R07 method seed；
- access-only 不送功法；
- 无灵根 / 未知功法拒绝；
- 六门静态定义；
- 灵根倍率与属性契合；
- 静心守一、赤阳灵体、玄阴灵体；
- 不相关体质无额外加速；
- 不同 qiDensity 与青云权限；
- 1 / 3 / 10 / 30 日；
- 引气入体；
- 跨炼气小层；
- 九层 100% 封顶；
- 寿终优先；
- save / reload 深拷贝；
- Session replay 与 `RESULT_PENDING`。

最终 R16 功能 CI：

- run：`31884102346`
- verify job：`95010735103`
- Typecheck：通过；
- Test：通过；
- Build：通过。

---

## 当前唯一状态与调度规则

```text
UI / feature
→ SessionCommand
→ GameAction / 对应 resolver
→ 唯一 GameState
→ debug log / digest / replay
→ PersistentGame
→ auto-save
```

禁止 React 直接 mutate 核心状态、页面直接写 localStorage、第二套 GameState/store、实现轮临时编造未冻结内容、绕过 replay/persistence。

---

## 当前迁移主线

```text
出生三选一 ✅
→ 童年关键节点 ✅
→ 成年 / 入道入口 ✅
→ 固定世界骨架 ✅
→ 地点知识状态 ✅
→ 节点旅行与时间 ✅
→ 区域页面 + 探索动作 ✅
→ 随机子地点 ✅
→ C13 沉脉石室内容冻结 ✅
→ R13 沉脉石室秘境最小闭环 ✅
→ R14 背包与储物袋 ✅
→ R15 装备栏与品阶 ✅
→ C16 修炼与突破内容冻结 ✅
→ R16 基础修炼 ✅
→ R17 功法系统
→ R18 炼气→筑基突破系统
→ R19 寿元 / 延寿 / 金丹
→ 后续战斗 / 宗门 / 职业 / 世界事件
```

## 下一步

执行：

> **R17｜功法系统**

R17 继续扩展 R16 同一套 `state.cultivation`，完成主修 / 辅修、熟练度四阶段与改修成本的正式结构。

重要边界：

- 已经真实掌握的功法可以进入熟练度与改修逻辑；
- 没有具体世界来源的功法，不得为了展示 R17 功能凭空加入玩家已知列表；
- 购买、拜师、宗门传功、遗迹得法、杀人夺宝等获取来源，只有在对应地点 / NPC / 事务已经形成真实入口后才能写入玩家状态；
- R17 不实现 R18 筑基突破，不使用凝基丹 / 破障丹突破，不进入金丹。

具体范围以 `CURRENT_TASK.md` 为准。
